{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "watchPatterns": ["frontend/**"]
  },
  "deploy": {
    "startCommand": "node_modules/.bin/serve dist -s -l $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
